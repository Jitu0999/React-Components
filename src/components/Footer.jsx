const Footer = () => {
  return (
    <div>
      Footer (The website footer is the bottom section, offering essential info (copyright, contact) and navigation (sitemap).It can also include social media links )
    </div>
  )
}

export default Footer
